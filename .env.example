# Copy to .env and fill in. Never commit .env.
# CTOC is a static prototype with mock data — no secrets are required to run it.
# These are only for optional tooling (Figma sync, AI assistants).

# FIGMA_TOKEN=          # for Figma MCP / design sync (file i3YWhwkly7iJEmk0tK3YJ7)
# ANTHROPIC_API_KEY=
# GITHUB_PAGES=true     # set by CI; toggles basePath=/CTOC for the Pages build
